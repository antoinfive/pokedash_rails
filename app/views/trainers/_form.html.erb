<%= form_for(@trainer) do |f| %>
  <% if @trainer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trainer.errors.count, "error") %> prohibited this trainer from being saved:</h2>

      <ul>
      <% @trainer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <h4>Add Pokemon to your Dashboard</h4>
  <div class="field">
    <%= f.collection_check_boxes(:pocketmonster_ids, Pocketmonster.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
