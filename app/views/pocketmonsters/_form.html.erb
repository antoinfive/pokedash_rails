<%= form_for(@pocketmonster) do |f| %>
  <% if @pocketmonster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pocketmonster.errors.count, "error") %> prohibited this pocketmonster from being saved:</h2>

      <ul>
      <% @pocketmonster.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :kind  %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to "Home", root_path %>
